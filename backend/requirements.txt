# Grid Infrastructure Anomaly Prediction — Backend Dependencies
# Phase 0: Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
pydantic>=2.10.0

# Phase 2: Model serving (uncomment when ready)
# transformers>=4.51.3
# torch>=2.6.0
# accelerate>=1.2.0
# diffusers>=0.34.0
# huggingface-hub>=0.28.0

# Phase 2: vLLM client for Cosmos Reason
httpx>=0.28.0

# Phase 2: Cosmos3-Nano video generation (via diffusers)
# Uncomment when ready to run on GPU:
# diffusers>=0.38.0
# accelerate>=1.2.0
# av>=16.0
# imageio>=2.37.0
# imageio-ffmpeg>=0.6.0

# Phase 4: Video processing
opencv-python-headless>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
